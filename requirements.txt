# Streamlit Cloud reads this for deployment.
# Keep in sync with pyproject.toml [project.dependencies].
# Dev deps (pytest, ruff, mypy, etc.) live only in pyproject.toml [project.optional-dependencies.dev].
streamlit>=1.35
pydantic>=2
pydantic-settings>=2
pyyaml>=6
structlog>=24
pandas>=2.1
numpy>=1.26
yfinance>=0.2.40
pyarrow>=15
httpx>=0.27
tenacity>=8
typer>=0.12
rich>=13
openai>=1.30
